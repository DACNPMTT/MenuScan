meta {
  name: Delete Menu
  type: http
  seq: 8
}

delete {
  url: {{base_url}}{{api_prefix}}/menus/{{menu_id}}
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{access_token}}
}

tests {
  test("soft deletes menu", function () {
    expect(res.status).to.equal(204);
  });
}
