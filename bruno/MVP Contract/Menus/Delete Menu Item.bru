meta {
  name: Delete Menu Item
  type: http
  seq: 7
}

delete {
  url: {{base_url}}{{api_prefix}}/menus/{{menu_id}}/items/{{menu_item_id}}
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{access_token}}
}

tests {
  test("deletes menu item", function () {
    expect(res.status).to.equal(204);
  });
}
