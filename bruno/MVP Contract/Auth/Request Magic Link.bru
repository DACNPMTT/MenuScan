meta {
  name: Request Magic Link
  type: http
  seq: 1
}

post {
  url: {{base_url}}{{api_prefix}}/auth/magic-links
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "email": "{{user_email}}"
  }
}

vars:pre-request {
  user_email: nguyentrongduc447@gmail.com
  base_url: http://localhost:8000
  api_prefix: /api/v1
}
