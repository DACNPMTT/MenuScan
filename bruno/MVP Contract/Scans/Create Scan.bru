meta {
  name: Create Scan
  type: http
  seq: 1
}

post {
  url: {{base_url}}{{api_prefix}}/scans
  body: multipartForm
  auth: none
}

headers {
  Authorization: Bearer {{access_token}}
}

body:multipart-form {
  file: @
  target_language: en
}
